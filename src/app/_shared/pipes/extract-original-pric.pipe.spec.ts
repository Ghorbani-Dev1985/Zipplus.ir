import { ExtractOriginalPricePipe } from './extract-original-price.pipe';

describe('ExtractOriginalPricePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractOriginalPricePipe();
    expect(pipe).toBeTruthy();
  });
});
